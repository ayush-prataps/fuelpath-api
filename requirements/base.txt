# Core framework
Django==6.1.1
djangorestframework==3.18.1
django-environ==0.12.0

# Database
psycopg[binary]==3.2.10       # psycopg3 – async-ready Postgres driver

# Optional: Redis cache backend (only needed if REDIS_URL is set in .env)
# django-redis==5.4.0
# redis==5.2.1
django-extensions==3.2.3

# Geospatial (PostGIS + GDAL wrappers)
#   Requires GDAL system library: brew install gdal  /  apt install gdal-bin
#   Uncomment once GDAL is installed in your environment.
# GDAL==3.10.3
# django.contrib.gis is enabled in settings when GDAL is available.

# Spatial indexing & scientific math
scipy==1.18.1

# HTTP / networking
httpx==0.28.1                 # async-capable HTTP client for OSRM calls
tenacity==9.1.2               # retry logic for external service calls

# Geocoding (offline pre-processing of the CSV, not on the hot path)
geopy==2.4.1

# Utilities
python-dotenv==1.1.1
