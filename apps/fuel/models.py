"""
Fuel API models.

The fuel app is intentionally thin — it owns no domain models of its own.
Domain data (fuel stations, cached routes) is owned by the apps that
manage that data (ingestion, routing).
"""
# No models defined here yet.
